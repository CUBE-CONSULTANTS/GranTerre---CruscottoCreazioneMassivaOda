_schema-version: "3.2"
ID: granterre.creazionemassiva
description: Generated by Fiori Tools
version: 0.0.1
modules:
# - name: granterre.creazionemassiva-approuter
#   type: approuter.nodejs
#   path: router
#   parameters:
#     disk-quota: 256M
#     memory: 256M
#   requires:
#     - name: granterre.creazionemassiva-repo-runtime
#     - name: granterre.creazionemassiva-uaa
#     - name: granterre.creazionemassiva-destination-service
#     - name: granterre.creazionemassiva-connectivity
- name: granterre.creazionemassiva-destination-content
  type: com.sap.application.content
  requires:
  - name: granterre.creazionemassiva-destination-service
    parameters:
      content-target: true
  - name: granterre.creazionemassiva-repo-host
    parameters:
      service-key:
        name: granterre.creazionemassiva-repo-host-key
  - name: granterre.creazionemassiva-uaa
    parameters:
      service-key:
        name: granterre.creazionemassiva-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: granterre.creazionemassiva_html_repo_host
          ServiceInstanceName: granterre.creazionemassiva-html5-srv
          ServiceKeyName: granterre.creazionemassiva-repo-host-key
          sap.cloud.service: granterre.creazionemassiva
        - Authentication: OAuth2UserTokenExchange
          Name: granterre.creazionemassiva_uaa
          ServiceInstanceName: granterre.creazionemassiva-xsuaa-srv
          ServiceKeyName: granterre.creazionemassiva-uaa-key
          sap.cloud.service: granterrec.ruscotto.lasafo
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: granterre.creazionemassiva-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: granterre.creazionemassiva-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - granterre.creazionemassiva.zip
      name: granterre.creazionemassiva
      target-path: resources/
- name: granterre.creazionemassiva
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: granterre.creazionemassiva-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: granterre.creazionemassiva-destination-service
    service-plan: lite
- name: granterre.creazionemassiva-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: granterre.creazionemassiva-xsuaa-srv
    service-plan: application
- name: granterre.creazionemassiva-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: granterre.creazionemassiva-html5-srv
    service-plan: app-host
- name: granterre.creazionemassiva-repo-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service-plan: app-runtime
    service: html5-apps-repo
- name: granterre.creazionemassiva-connectivity
  type: org.cloudfoundry.managed-service
  parameters:
    service-plan: lite
    service: connectivity

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true